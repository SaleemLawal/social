services:
  api:
    build:
      context: .
      target: dev
    volumes:
      - ./:/app
      - /app/bin
      - /app/tmp
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build

volumes:
  go-mod-cache:
  go-build-cache:
